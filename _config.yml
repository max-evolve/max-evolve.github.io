
#configs
title: mayankkhera.com
email:
twitter_username: username
github_username: username


# Build settings
markdown: kramdown
remote_theme: mmistakes/minimal-mistakes
minimal_mistakes_skin: default
search: false

# Outputting
permalink: /:categories/:title/
# paginate disabled — Thoughtfolio (/posts/) is the single post index
# paginate: 5
# paginate_path: /page:num/
timezone: # https://en.wikipedia.org/wiki/List_of_tz_database_time_zones

include:
  - _pages
  - _posts
  - _resources
  - _books

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:


# Plugins (previously gems:)
plugins:
  - jekyll-remote-theme
  - jekyll-sitemap
  - jekyll-gist
  - jekyll-feed
  - jemoji
  - jekyll-include-cache

author:
  name   : "Mayank Khera"
  avatar : "/assets/images/mayank-profile.jpg"
  bio    : "I am a product builder who enjoys writing about technology and career growth"
  location: "London, UK"
  linkedin: mayankkhera
  

defaults:
  
  # _posts
  - scope:
      path: ""
      type: posts
    values:
      layout: single
      author_profile: false
      classes: no-sidebar
      read_time: false
      show_date: true
      comments: false
      share: false
      related: false
      
      
  
  # _pages
  - scope:
      path: "_pages"
      type: pages
    values:
      layout: single
      author_profile: true
      

  # resources
  - scope:
      path: ""
      type: resources
    values:
      layout: single
      related: false
      author_profile: false
      show_date: true
      
      

  # books
  - scope:
      path: ""
      type: books
    values:
      layout: single
      related: false
      author_profile: false
      

        
# collections
collections:
  resources:
    output: true
  books:
    output: true
    permalink: /books/:title/
